{
  "name": "puma-json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server-auth db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3",
    "json-server-auth": "^2.1.0"
  }
}
